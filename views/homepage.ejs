<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="homepage_style.css">
    <link rel="icon" type="image/x-icon" href="weight.svg">
    <title>Homepage</title>
</head>
<body>
    <nav>
        <ul>
            <li><a onclick="location.reload();">Hi <%= username %>!</a></li>
            <li><a href="newWorkout">New Workout</a></li>
            <li><a href="new_workout">Compete</a></li>
            <li><a href="leaderboard">Leaderboard</a></li>
            <li><a href="login">Logout</a></li>
        </ul>
    </nav>
    <tr>
        <td><%= user.username %></td>
        <td><%= user.password %></td>
        <td><%= user.workouts && user.workouts.length > 0 ? user.workouts.join(', ') : 'No workouts recorded' %></td>
        <td><%= user.weights && user.weights.length > 0 ? user.weights.join(', ') : 'No weights recorded' %></td>
    </tr>
</body>
</html>