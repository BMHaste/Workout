<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Welcome <%= username %>!</h1>
    <form id="form" action="/users/homepage" method="POST">
        <div>
            <input type="text" name="workout" id="workout-input" placeholder="Workout">
        </div>
        <div>
            <input type="text" name="weight" id="weight-input" placeholder="Weight">
        </div>
        <button type="submit">Submit</button>
    </form>
    <a href="leaderboard" class="button">Leaderboard</a>

    <tr>
        <td><%= user.username %></td>
        <td><%= user.password %></td>
        <td><%= user.workouts && user.workouts.length > 0 ? user.workouts.join(', ') : 'No workouts recorded' %></td>
        <td><%= user.weights && user.weights.length > 0 ? user.weights.join(', ') : 'No weights recorded' %></td>
    </tr>
</body>
</html>